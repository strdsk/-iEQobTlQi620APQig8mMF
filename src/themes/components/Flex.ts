import type { StyleConfig } from '@chakra-ui/theme-tools'

const Flex: StyleConfig = {
  variants: {
    'section': {
      as: 'section',
      justifyContent: 'center',
    }
  }
}

export default Flex